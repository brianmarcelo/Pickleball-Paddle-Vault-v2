/**
 * Paddle Vault - Application Logic (app.js)
 */

document.addEventListener('DOMContentLoaded', () => {
    initApp();
});

function initApp() {
    // Logic extracted from index.html...
    console.log("App initialized with " + PADDLES.length + " paddles.");
    // ...
}
